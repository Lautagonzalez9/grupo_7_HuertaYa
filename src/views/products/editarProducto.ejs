<!DOCTYPE html>
<html lang="en">
 <%- include('../partials/head') %>
    <title>Document</title>
 </head> 
<body>
    <%- include('../partials/header') %>


<main class="cont-form">

    <form action="/admin/edit/<%= productos.id %>"  method="POST" class="formulario-registro">

      <h3>Estas editando: <i><%= locals.productos?.name %></i></h3>


     <label>Nombre del producto</label>
        <input type="text" name="Nombre-del-producto" placeholder="Nombre del producto" class="nombre" value="<%= locals.productos?.name %> ">
     
     <label for="comentario">Descripción</label>
        <textarea name="comentario" id="comentario" placeholder="Ingrese la descripción del producto" value="<%= locals.productos?.descripcion %> " ></textarea>
     <label>Categoría</label>
         <select name="category" placeholder="Seleccione una categoria">
            <option value="Frutas" selected="<%= productos.category == 'Frutas' ? true : null %>">Frutas</option>
            <option value="Verduras" selected="<%= productos.category == 'Verduras' ? true : null %>">Verduras</option>
            <option value="Verduras cortadas" selected="<%= productos.category == 'Verduras cortadas' ? true : null %>">Verduras cortadas</option>
            <option value="Bolsones" selected="<%= productos.category == 'Bolsones' ? true : null %>">Bolsones</option>
            <option value="Productos orgánicos" selected="<%= productos.category == 'Productos orgánicos' ? true : null %>">Productos orgánicos</option>
         </select>
     
     <label>Presentación</label>
         <input type="text" name="unidad" placeholder="Ingrese el tipo de presentación del producto, ej: kg, bolsa, atado..." value="<%= locals.productos?.presentation%> ">
     
     <label>Precio unitario</label>
        <input type="text" name="precio" placeholder="Ingrese el precio unitario del producto" value="<%= locals.productos?.price%> ">
     
     <label>Imagen del producto</label>
        <input type="file" name="imagen" accept=".jpg, .png" multiple>   
     
     <button type="submit" class="button">Guardar Cambios</button>
     <form action="/admin/delete/<%= productos.id %>?_method=DELETE" method="POST">
      <button type="submit" class="button">ELIMINAR</button>
     </form> 
   
     

    </form>
   
</main>

<%- include('../partials/footer') %>
</body> 
</html>