<!DOCTYPE html>
<html lang="en">
 <%- include('../partials/head') %>
    <title>Document</title>
 </head> 
<body>
    <%- include('../partials/header') %>
<main class="cont-form">
    <form action="/admin/editar/:id/editar?_method=PUT" method="POST" class="formulario-registro">

     <label>Nombre del producto</label>
        <input type="text" name="Nombre-del-producto" placeholder="Nombre del producto" class="nombre" value="<%= locals.productos?.name %> ">
     
     <label for="comentario">Descripción</label>
        <textarea name="comentario" id="comentario" placeholder="Ingrese la descripción del producto" value="<%= locals.productos?.descripcion %> " ></textarea>
     <label>Categoría</label>
         <select name="categoria" placeholder="Seleccione una categoria">
            <option value="frutas" selected="<%= locals.productos.category == 'frutas' ? true : null %> ">Frutas</option>
            <option value="verduras" selected="<%= locals.productos.category == 'verduras' ? true : null %> ">Verduras</option>
            <option value="verduras-cortadas" selected="<%= locals.productos.category == 'verduras-cortadas' ? true : null %> ">Verduras cortadas</option>
            <option value="bolsones" selected="<%= locals.productos.category == 'bolsones' ? true : null %> ">Bolsones</option>
            <option value="organico" selected="<%= locals.productos.category == 'organico' ? true : null %> ">Productos orgánicos</option>
         </select>
     
     <label>Presentación</label>
         <input type="text" name="unidad" placeholder="Ingrese el tipo de presentación del producto, ej: kg, bolsa, atado..." value="<%= locals.productos?.presentation%> ">
     
     <label>Precio unitario</label>
        <input type="text" name="precio" placeholder="Ingrese el precio unitario del producto" value="<%= locals.productos?.price%> ">
     
     <label>Imágen del producto</label>
        <input type="file" name="imagen" accept=".jpg, .png" multiple>   
     
     <button type="submit" class="button">Guardar Cambios</button>

    </form>
   
</main>
<%- include('../partials/footer') %>
</body> 
</html>