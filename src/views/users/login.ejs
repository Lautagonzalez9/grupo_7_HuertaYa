<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/stylesLogin.css">
    <title>Login</title>
<body>
      <%- include('../partials/header')%>
    <main>
        <div class="contenedor-formulario contenedor">
            <div class="erroresLogin">
                <ul style="color:red ;"></ul>
            </div>
            <div class="imagen-formulario">

            </div>
            <form class="formulario" action="/login" method="POST">
                <div class="texto-formulario">
                    <h2> ¡Bienvenido de nuevo!</h2>
                    <p> Inicia seccion con tu cuenta</p>
                </div>
                <div class="input-detalles">
                    <label for="email">Usuario</label>
                    <input type="email" placeholder="Correo Electronico" name="email" class="correo" value= <%= locals.old ? old : '' %>>
                    <% if (locals.errors && errors.email) { %>
                        <div style="color: red;"> <%= errors.email.msg %></div>
                    <% } %> 
                </div>
                <div class="input-detalles">
                    <label for="password">Password</label>
                    <input type="password" name="password" class="contraseña" placeholder="Password" value= <%= locals.old ? old : '' %>>
                    <% if (locals.errors && errors.contrasenia) { %>
                        <div style="color: red;"> <%= errors.contrasenia.msg %></div>
                    <% } %> 
                </div>
                <div class="recordarme">
                    <label for="recordarme">Recordarme</label>
                    <input type="checkbox" name="recordarme" id="recordarme">
                </div>
                <div class="input-detalles">
                    <button type="submit">LOGIN</button>
                </div>

                <div class="password-texto">
                    <a href="#">¿Ólvido su passaword?</a>
                </div>


            </form>
        </div>
    </main>
    <script src="/js/login-validation.js" ></script>
    <footer>
        <%- include("../partials/footer")%>
    </footer>
</body>

</html>